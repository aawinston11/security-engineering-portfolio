FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir \
    fastapi==0.115.0 \
    uvicorn[standard]==0.30.6 \
    pydantic==2.8.2
COPY src/siem_mock /app/siem_mock
COPY data /app/data
EXPOSE 8000
CMD ["uvicorn", "siem_mock.app:app", "--host", "0.0.0.0", "--port", "8000"]
